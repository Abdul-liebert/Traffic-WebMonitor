/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // 'class' atau 'media' (gunakan 'class' untuk kontrol manual)
  content: ["./src/**/*.{html,js}"],
  theme: {
    extend: {},
  },
  plugins: [],
}